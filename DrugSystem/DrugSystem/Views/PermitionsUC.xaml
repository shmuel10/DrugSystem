<UserControl x:Class="DrugSystem.PermitionsUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrugSystem">
    <GroupBox Header="הרשאות" FlowDirection="RightToLeft" Style="{x:Null}">
        <StackPanel>
            <CheckBox x:Name="Prescription" Content="הנפקת מרשמים" IsChecked="{Binding CanAddPrescription}"/>
            <CheckBox x:Name="Doctor" Content="הוספת רופא חדש" IsChecked="{Binding CanAddDoctor}"/>
            <CheckBox x:Name="Patient" Content="הוספת מטופל חדש" IsChecked="{Binding CanAddPatient}" />
            <CheckBox x:Name="Medicine" Content="הוספת תרופה למערכת" IsChecked="{Binding CanAddMedicine}"/>
        </StackPanel>
    </GroupBox>
</UserControl>
